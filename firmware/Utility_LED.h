#ifndef LED_H
#define LED_H

#include <Arduino.h>

namespace LED {
  void Blink(float intervalSec);
  void On();
  void Off();
}

#endif // LED_H
                                                                              
