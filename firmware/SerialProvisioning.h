#ifndef SERIAL_PROVISIONING_H
#define SERIAL_PROVISIONING_H

namespace SerialProvisioning {
  // Accept newline-delimited Studio commands without blocking the main loop.
  void maintain();
}

#endif // SERIAL_PROVISIONING_H
